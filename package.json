{
  "name": "company-it-service-platform",
  "version": "0.1.0",
  "private": true,
  "description": "Portfolio-ready internal IT service platform with helpdesk tickets, asset management, onboarding workflows, and a RAG-style support copilot.",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently -n api,web -c cyan,magenta \"npm run dev -w apps/api\" \"npm run dev -w apps/web\"",
    "dev:api": "npm run dev -w apps/api",
    "dev:web": "npm run dev -w apps/web",
    "build": "npm run build -w packages/shared && npm run build -w apps/api && npm run build -w apps/web",
    "test": "npm run test -w apps/api",
    "typecheck": "npm run build -w packages/shared && npm run build -w apps/api && npm run typecheck -w apps/web",
    "format": "prettier --write ."
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "prettier": "^3.5.3",
    "typescript": "^5.8.3"
  }
}

